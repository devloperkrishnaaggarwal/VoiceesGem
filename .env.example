# MODEL SELECTION
# =============================================================================
# "ga" = Vertex AI with GA model (production stable)
# "preview" = Google AI API with preview model (has thinking mode + tools)
# FOR MIKE'S REAL ESTATE AGENT: YOU MUST USE "preview"
GEMINI_MODEL=preview


# =============================================================================
# GOOGLE AI API & TOOLS (Required for preview model + Real Estate Tools)
# =============================================================================
# 1. API Key for Gemini Live Preview
# Get your API key from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-ai-api-key

# 2. Google User OAuth Credentials (Calendar, Gmail, Sheets)
# These are required for Mike's Agent to book meetings, send emails, and add leads.
# You can get these by setting up an OAuth Client ID in Google Cloud Console.
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REFRESH_TOKEN=your-refresh-token

# 3. Google Sheet for Leads
# The ID of the Google Sheet to store leads in (from the URL)
GOOGLE_SHEET_ID=your-sheet-id

# Thinking mode (optional)
# THINKING_BUDGET_TOKENS=0  # 0=disabled

# =============================================================================
# BOT CONFIGURATION
# =============================================================================
# Name the bot uses when introducing itself
BOT_NAME=Sarah
PROMPT_SOURCE=local